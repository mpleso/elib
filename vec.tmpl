{{/* -*- mode: Go -*- */}}

{{if ne .TAGS ""}}
//+build {{.TAGS}}
{{end}}

{{define "elib"}}{{if ne . "elib"}}elib.{{end}}{{end}}

package {{.Package}}

{{if ne .Package "elib"}}
import (
	"github.com/platinasystems/elib"
)
{{end}}

type {{.VecType}} []{{.Type}}

func (p * {{.VecType}}) Resize(n uint) {
	c := {{template "elib" .Package}}Index(cap(*p))
	l := {{template "elib" .Package}}Index(len(*p)) + {{template "elib" .Package}}Index(n)
	if l > c {
		c = {{template "elib" .Package}}NextResizeCap(l)
		q := make([]{{.Type}}, l, c)
		copy(q, *p)
		*p = q
	}
	*p = (*p)[:l]
}

func (p * {{.VecType}}) validate(i uint, zero *{{.Type}}) *{{.Type}} {
	c := {{template "elib" .Package}}Index(cap(*p))
	l := {{template "elib" .Package}}Index(i) + 1
	if l > c {
		cNext := {{template "elib" .Package}}NextResizeCap(l)
		q := make([]{{.Type}}, cNext, cNext)
		copy(q, *p)
		if zero != nil {
			for i := c; i < cNext; i++ {
				q[i] = *zero
			}
		}
		*p = q[:l]
	}
	if l > {{template "elib" .Package}}Index(len(*p)) {
		*p = (*p)[:l]
	}
	return &(*p)[i]
}
func (p * {{.VecType}}) Validate(i uint) *{{.Type}} { return p.validate(i, (*{{.Type}})(nil)) }
func (p * {{.VecType}}) ValidateInit(i uint, zero {{.Type}}) *{{.Type}} { return p.validate(i, &zero) }

func (p {{.VecType}}) Len() uint { return uint(len(p)) }
