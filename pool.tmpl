{{/* -*- mode: Go -*- */}}

{{if ne .TAGS ""}}
//+build {{.TAGS}}
{{end}}

{{define "elib"}}{{if ne . "elib"}}elib.{{end}}{{end}}
{{define "T"}}{{.}}Pool{{end}}

package {{.Package}}

{{if ne .Package "elib"}}
import (
	"github.com/platinasystems/elib"
)
{{end}}

type {{template "T" .ID}} struct {
	{{template "elib" .Package}}Pool
	{{.Data}} {{.Type}}
}

func (p * {{template "T" .ID}}) GetIndex() (i uint) {
	l := uint(len(p.{{.Data}}))
	i = p.Pool.GetIndex(l)
	if i >= l {
		p.Validate(i)
	}
	return i
}

func (p * {{template "T" .ID}}) PutIndex(i uint) (ok bool) {
	return p.Pool.PutIndex(i)
}

func (p * {{template "T" .ID}}) IsFree(i uint) (ok bool) {
	return p.Pool.IsFree(i)
}

func (p * {{template "T" .ID}}) Resize(n int) {
	c := {{template "elib" .Package}}Index(cap(p.{{.Data}}))
	l := {{template "elib" .Package}}Index(len(p.{{.Data}}) + n)
	if l > c {
		c = {{template "elib" .Package}}NextResizeCap(l)
		q := make({{.Type}}, l, c)
		copy(q, p.{{.Data}})
		p.{{.Data}} = q
	}
	p.{{.Data}} = p.{{.Data}}[:l]
}

func (p * {{template "T" .ID}}) Validate(i uint) {
	c := {{template "elib" .Package}}Index(cap(p.{{.Data}}))
	l := {{template "elib" .Package}}Index(i) + 1
	if l > c {
		c = {{template "elib" .Package}}NextResizeCap(l)
		q := make({{.Type}}, l, c)
		copy(q, p.{{.Data}})
		p.{{.Data}} = q
	}
	if l > {{template "elib" .Package}}Index(len(p.{{.Data}})) {
		p.{{.Data}} = p.{{.Data}}[:l]
	}
}

func (p * {{template "T" .ID}}) Elts() int {
	return len(p.{{.Data}}) - p.FreeLen()
}
